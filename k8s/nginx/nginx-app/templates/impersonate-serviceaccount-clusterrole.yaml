apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  # A name for the ClusterRole
  name: impersonate-serviceaccount
rules:
  - apiGroups: [""]
    resources: ["serviceaccounts"]
    verbs: ["impersonate"]
